<%= do_new({
    title: t(".pages"),
    columns: [
      :title,
      Btemplater::NewDecorator.new(
        :content,
        Proc.new do |f, c|
          f.input :content, :as => :ckeditor, :input_html => { :ckeditor => {:toolbar => 'Full'} }
        end
      )
    ],
    item: @obj,
    model: Conratepages::Page,
    url: conratepages.pages_path,
    back_url: main_app.home_path,
    method: :post
  })
%>