<%= do_index({
  title: t(".pricemanipulators"),
  columns: [
    :id,
    :name,
    :perproduct
  ],
  items: @objects,
  model: Conratewebshop::Pricemanipulator,
    actions: [
      :show,
      :edit,
      :destroy
    ]
  })
%>

<%= do_new_button({model: Conratewebshop::Pricemanipulator.new}) %>
